# Taste Profile
- **Prefers architectural rethinking over patching when state/store/persistence boundaries become tangled** — when the architecture becomes fragile (e.g., tabs, stores, and persistence concerns are tightly coupled and causing crashes), prefers to step back and fundamentally redesign the approach rather than applying incremental fixes like ErrorBoundaries or try/catch wrappers. Signals this with "i will have to rethink" rather than asking for another workaround. Confidence: 0.6
- Prefers **local-first architecture** — SQLite is the source of truth, cloud sync is optional/posterior, not foundational. Confidence: 0.9
- Uses **files for content, SQLite for metadata** (hybrid storage: prompt JSON, markdown, canvas, attachments on disk; documents, versions, templates, collections in DB). Confidence: 0.9
- Keeps **Rust minimal** — used only for SQLite, filesystem, and native APIs; no Rust AI, no Rust embeddings. Confidence: 0.9
- Places **AI logic in TypeScript**, never in Rust. Confidence: 0.9
- Uses **one SQLite database per workspace** (self-contained workspaces). Confidence: 0.9
- Prefers **deterministic context library over probabilistic RAG** — namespace-based assets (@persona, @project, @rules, @variables) instead of embeddings/vector search. Confidence: 0.9
- Uses **folder-based scratchpad** per document (notes.md, canvas.json, attachments/, metadata.json). Confidence: 0.8
- Structures workspaces as **self-contained directories** with `.prmptly/` internal folder. Confidence: 0.9
- Uses a **section model only for prompts/templates**, where it earns its keep (templates-as-blueprints, per-section comments, output wrapping, `templateSectionId` mapping). Quicks deliberately do **NOT** use the section model — this revises the earlier "single section model shared across templates, documents, and quicks" decision. Confidence: 0.85
- **Quicks are a single markdown document — one editor, one body** — sections were only justified by reordering, which the user judged "tedious and low-value," so quicks hold one markdown string and `plain` output is the markdown verbatim (ready to paste into an LLM). Sections become a **derived view** for quicks: `## ` headings are parsed on demand (e.g., outline, or quick→prompt promotion), never stored as the primary quicks model. Confidence: 0.8
- **Markdown is the storage/source-of-truth format** — for quicks (and by extension the app's content boundaries), markdown is the canonical persisted/edited format while structured representations (sections, plain/JSON/XML, Tiptap JSON) are derived from it rather than stored as parallel hand-rolled models. Confidence: 0.7
- **Prefers a real markdown parser + writer over hand-rolled serializers** — explicitly rejects the custom `mdToHtml`/`serializeDoc`/`nodeToXml`-style half-parsers in favor of a proper markdown library (e.g., `marked`/`turndown` or a Tiptap markdown extension), consolidating to one canonical representation per boundary and eliminating union types/guards. Confidence: 0.7
- Distinguishes **quicks from prompts by UI only** — both use the same document schema with different `type` field. Confidence: 0.9

- **Quicks and prompts have strict UI separation on separate sidebars** — quicks (type='quick') belong exclusively in the Home tab's quicks section and must never appear in the prompt sidebar. The prompt sidebar should filter to only `type='prompt'` documents. Any mixing of the two in the same sidebar view is considered a bug. Confidence: 0.9
- Models **templates as blueprints** that are cloned to create documents; templates never change after creation. Confidence: 0.9
- Avoids `vault_id` — removed from schema entirely. Confidence: 0.8
- Uses **crypto.randomUUID()** for ID generation (not `uuid` package, not `nanoid`). Confidence: 0.8
- Uses **domain-driven file organization** in `lib/db/`, `lib/types/`, `services/`, `hooks/store/`, `components/`. Confidence: 0.8
- Prefers **service layer** between DB and components (e.g., `services/service.document.ts`). Confidence: 0.8
- Uses **Zustand stores** for state management. Confidence: 0.8
- Prefers **barrel exports** from domain modules (e.g., `lib/db/index.ts` exports `getDB`). Confidence: 0.7
- Uses **Tauri v2** for desktop shell. Confidence: 0.9
- **Traces end-to-end data flow systematically** before fixing bugs — reads through component → store → service → DB chains to understand the full path before making changes. Confidence: 0.7

- **Verifies all stale references are cleaned up after refactoring by grepping for removed identifiers** — after moving a state variable from local `useState` to the store (or any similar refactor that removes old API calls), searches the entire file(s) for remaining references to the deleted variable/function (e.g., `setHasContent`) and eliminates them all. Does not assume the first edit caught every call site. Confidence: 0.7

- **Requests comprehensive, structured project context documentation when stepping back to rethink a complex/critical issue** — when the user says "i need to take think clearly state the whole picture clearly give whole context what we are doing what we are using everything", they want a detailed architectural overview covering: what the project is, tech stack (with versions), architecture (with directory/file layout), UI layout/panels, editing modes and their differences, what's working, what's fragile (with root cause analysis), and a file inventory organized by domain. This context is meant to help them think through the problem clearly and potentially share with another LLM for advice. The expected format is structured markdown with sections, tables, bullet points, and code blocks for directory listings — a comprehensive "state of the project" document. Confidence: 0.8

- **Applies runtime type guards (`typeof`) before operating on union-typed data from dynamic sources (paste, deserialization, async fetch)** — when a field can hold either a `string` (from markdown paste/import) or a rich-text `JSONContent` object (from Tiptap), explicitly checks `typeof value === "string"` before calling methods like `serializeDoc()` that expect objects. Does not assume deserialization/parsing always produces the object form. Proactively fixes the same class of bug in all code paths (`compile()` and `persist()`) where the pattern occurs. Confidence: 0.7
- **Preserves unused components on disk** rather than deleting them — keeps code around for future features (e.g., kept `AddContextPanel`, `EnableContext`, `ScopeDocumentsPanel` even after RAG was disabled). Confidence: 0.8
- Runs **`tsc --noEmit` type checking** as part of the workflow to verify code correctness before proceeding. Confidence: 0.8

- **Provides requirements as comprehensive structured markdown architectural specifications** — communicates feature requests as detailed specs with section hierarchy, architecture diagrams (ASCII), deliverables checklist, explicit design constraints, and clear "what goes where" for file organization. The specification leaves minimal ambiguity about the desired architecture. Confidence: 0.8

- **Prefers provider abstraction with registry/extensibility pattern for modular features** — explicitly wants a unified interface (`AIProvider`) that multiple implementations conform to, with a registry pattern (`registerProvider`/`getProvider`) so the rest of the application never knows which provider is being used. The same extensibility pattern is required for prompt actions ("future actions should be easy to register"). Confidence: 0.7

- **Mandates a single Context Builder entry point for prompt assembly** — the agent "should never manually concatenate strings throughout the codebase. Everything goes through the Context Builder." This ensures prompt construction is centralized, testable, and consistent rather than scattered across the codebase. Confidence: 0.8

- **Requires AbortController-based streaming with user-facing cancellation** — streaming responses must support partial token delivery to the UI, user cancellation mid-generation (via `AbortController`), and a responsive UI throughout. The streaming architecture must be provider-agnostic (provider switching should not affect the streaming architecture). Confidence: 0.8

- **API keys must be stored locally, never logged, never embedded in prompts** — explicit security principle: API keys stay in local SQLite storage only, must never appear in logs or console output, must never be included in prompt context sent to providers. Keys should be masked in the UI (e.g., `••••••••sk-...abcd`). Confidence: 0.9

- **Skips full production build (`vite build`) during Tauri development** — considers the full build step unnecessary for dev workflow in a Tauri context. `tsc --noEmit` is sufficient verification during active development; the full build is only needed for actual production/packaging. Confidence: 0.7
- Uses **React + TypeScript** for UI. Confidence: 0.9
- **Maintains visual consistency between sidebar panel and main view header** — tab icons/labels in the sidebar should be mirrored in the corresponding main view header. Confidence: 0.5
- Uses **Tiptap** for rich text editors. Confidence: 0.9
- Prefers **icon buttons with hover tooltips** over text labels for compact UI tab bars — uses lucide-react icons with a `group-hover:opacity-100` tooltip span positioned below the icon. Confidence: 0.7

- **Requires consistent custom CSS tooltip pattern across all UI, replacing native HTML `title` attributes** — all tooltips must use the same styled `<span>` approach (absolute positioned, `group-hover:opacity-100`, `bg-surface border border-border text-muted`, small text) rather than native `title` attributes. Explicitly mandates: "use consistent tooltip everywhere the one thats used for scratchpad, canvas etc." Applies to all icon buttons, action buttons, toggle buttons, and sidebar items throughout the entire application. Confidence: 0.8
- Prefers **spring-based micro-interactions on interactive UI elements** — uses `motion.button` with `whileTap={{ scale: 0.88 }}` and spring transitions (type: spring, stiffness: 500, damping: 20) for a smooth tactile feel on buttons, toggles, and tab icons. Confidence: 0.6
- Prefers **dedicated top-level tab per distinct feature** rather than burying tools inside other panels — each major tool (builder, scratchpad, canvas/agent builder, prompt) gets its own visible tab with a unique icon. Confidence: 0.7
- Uses **SQLite via Tauri plugin** (not better-sqlite3 or sql.js). Confidence: 0.9

- Prefers **keyboard shortcuts for common actions** — explicitly requested Ctrl+S for save/update operations and Ctrl+O for toggling the outline/table-of-contents panel, applied consistently across all relevant views (SnippetModal, FileTab/prompt builder, HomeView, OutlinePanel). Confidence: 0.7

- Prefers **idempotent save semantics** — Ctrl+S should update the existing document in-place, not create a duplicate. The first save creates the document, subsequent saves update it using the stored document ID. Confidence: 0.8

- Prefers **autosave after initial manual save** — once a document has been saved manually for the first time, debounced autosave (e.g., 3 seconds after last edit) should automatically persist changes going forward. Confidence: 0.7

- Prefers **fixed-width tabs with truncated labels** — open tabs in the tabbar should have a constrained max-width with text truncation/ellipsis rather than stretching to fit the full label text. Confidence: 0.7

- **Removes transitional/experimental UI elements when a feature is finalized** — considers cleaning up leftover UI scaffolding (e.g., an icon pill in the sidebar) as the last step before marking a feature "done". Does not leave behind feature-toggle-style visual cruft. Confidence: 0.5

- **Prefers minimal sidebar UI without category label headers** — sidebar panels should not display text labels like "Prompts", "Snippets", "Templates" as section headers; instead, keep only compact create/action buttons. Sidebar should be streamlined and uncluttered. Confidence: 0.7

- **Prefers inline content resolution over placeholder chips in editors** — when selecting a mention/snippet from a suggestion popup, resolve the content immediately and insert the actual text at cursor position, rather than inserting a pending chip/placeholder node that resolves asynchronously. Confidence: 0.9

- **Prefers content excerpts with truncation in suggestion/mention dropdowns** — show a truncated preview (e.g., 120 chars) of the content beneath each item label so users can identify what they're selecting at a glance. Confidence: 0.7

- **Prefers both Enter and Tab to confirm selections in suggestion popovers** — Tab key (alongside Enter) should populate/confirm the selected item, not just move focus. Confidence: 0.7

- **Omits backend/source implementation labels from user-facing UI** — labels like "deterministic" or "rag" that describe the internal source mechanism should not appear in menus, dropdowns, or any UI the user sees. Confidence: 0.6

- **Deletes the trigger syntax when inserting resolved content from mention popups** — when a user selects a mention/snippet from a suggestion list, the entire `@namespace:key` text (including the `@` symbol) must be removed from the document before inserting the resolved content, leaving no trigger syntax behind. Confidence: 0.8

- **Filters out implicit/internal synthetic scopes from user-facing dropdowns** — synthetic namespaces like `__global__` that are internal defaults (not user-created) should be hidden from namespace/scope selection menus and never shown as selectable options. Confidence: 0.7

- **Prefers flat key-value snippet model over namespaced/scoped snippets** — explicitly prefers simple key-value pairs for snippets (just "key" and "value" fields) rather than a hierarchical namespace:key model with scopes. Considers scopes unnecessary complexity. Confidence: 0.8

- **Prioritizes frontend simplification over backend data purity** — willing to remove features from the UI (scope/namespace fields, scope display in sidebar) even if the database schema still stores the legacy data. Pragmatic approach: "you dont have to do it from the db just remove it from the front." Confidence: 0.6

- **Expects live markdown/rich-text conversion parity between typing and pasting** — markdown syntax (e.g., `#` for headings) should convert to rich text in real-time as the user types, not only when content is pasted. The same formatting rules must apply to both input paths. Confidence: 0.7

- **Expects rich text formatting to be visually apparent immediately** — structural formatting changes (e.g., creating an h1/h2/h3 node via `# ` shortcut) must come with corresponding CSS styling so the user can see the effect. Without visible typographic differentiation (font-size, weight, margin), the formatting feature is considered broken/non-functional from the user's perspective — the node existing in the DOM is not enough. Applies to all rich text formatting (headings, lists, blockquotes, etc.). Confidence: 0.8

- **Shows content analytics (char count, word count, token estimate) as a subtle top bar in editor views** — wants quantitative writing metrics visible at a glance, especially token estimates relevant to LLM usage. The stats/analytics bar should be positioned at the **top** of the editor view (above the editor content), not at the bottom. Confidence: 0.8

- **Prefers ultra-compact styling for secondary/ambient metadata** — secondary info like stats bars should use minimum visual weight (`text-[10px] font-mono text-muted`, thin border separator) so it's available without competing with primary content. Confidence: 0.6

- **Token-aware — expects token estimates alongside raw char/word counts** — token estimation matters for an LLM-focused workflow; wants approximate token counts to manage context windows and budgets. Confidence: 0.7

- **Prefers full-height editor layouts with internal overflow scrolling** — editors should fill the available vertical space (flex-1, h-full) and scroll their content internally rather than being constrained to a fixed max-height. Content analytics bars stay pinned outside the scroll area (shrink-0 below it). Confidence: 0.7

- **Prefers full-width (edge-to-edge) layouts over centered/max-width-constrained layouts** — explicitly removes `max-w-4xl mx-auto` style constraints in favor of `w-full` for UI panels and editor views. Does not want content artificially centered or constrained horizontally. Confidence: 0.3

- **Expects visual/structural consistency between quicks/home editor and builder editor** — both views should use the same container pattern, section sizing, and layout approach. When a layout change is applied to one (e.g., full-width sections within a centered constrained container), the same change should be replicated in the other. Confidence: 0.6

- **Prefers stats bar visibility to derive from actual computed data, not state flags** — the visibility of the content analytics bar should react to the actual derived value (e.g., `allText.length > 0`) rather than a component state variable (e.g., `!output && allText`). Wants the bar to show/hide in real-time based on the actual content state, not a stale or indirectly related flag. Confidence: 0.7

- **Prefers live-editable document name in the file tab header** — the document name displayed in the tab header (FileTab top bar) should be an editable input bound to component state (`value`/`onChange`), not a static label. Editing the name should persist to the database and synchronize the tabbar label immediately (on blur or Enter key). The name shown must always correlate to the opened file. Confidence: 0.8

- **Expects CRUD operations (rename, delete) accessible via right-click context menus on sidebar tree items** — sidebar document/prompt items should support right-click context menus with "Rename" (inline editable in the sidebar — no popup dialog) and "Delete" (confirm + delete from DB) actions. Applies to both quicks and prompts in the sidebar. Confidence: 0.8

- **Prefers inline sidebar renaming over popup dialogs** — clicking "Rename" in a context menu should directly make the filename editable in-place in the sidebar tree (swap the `<span>` for an `<input>`), never use `window.prompt()` or any modal dialog. Should auto-focus, select all text, commit on Enter/blur (with save), and cancel on Escape. Confidence: 0.8

- **Expects the sidebar tree to re-fetch/refresh after CRUD mutations** — after renaming or deleting a document via context menu, the sidebar tree should reload (via a `onRefreshTree` callback threaded through the component chain) rather than relying on optimistic local state updates alone. Confidence: 0.7

- **Prefers a single persistence path with no duplicate autosave logic** — explicitly requires "no duplicate persistence logic" and a "single source of truth" for saving. There should be exactly one debounced autosave mechanism (e.g., the store's internal debounce), not multiple competing timers in different components. Confidence: 0.8

- **Store-driven view state must not be duplicated as local component state** — when a store action (e.g., sidebar "New Quick" button setting `hasContent: true` in the Zustand store) is intended to control view behavior (e.g., showing/hiding an action bar in HomeView), the view must read that state directly from the store, not maintain its own duplicate `useState` copy. Duplicate local state that mirrors store state inevitably desynchronizes — the store gets updated but the local state doesn't, causing the view to appear broken. The fix is to delete the local state and read from the store's corresponding field instead. Confidence: 0.8

- **Deletion must comprehensively remove all traces** — deleting a document should remove the database entry, associated files from disk, close any open tab, and update the sidebar tree in one operation. Leaves no orphaned state anywhere in the application. Confidence: 0.8

- **Prefers direct Zustand store mutations for synchronizing UI labels over relying on tree refresh** — tab labels, sidebar names, and document titles should stay in sync by directly mutating the store state (e.g., `useTabViewStore.setState`) rather than requiring a full tree refresh. Follows the "no manual refresh required" principle for data already available client-side. Confidence: 0.7

- **Expects all document name surfaces (sidebar tree item, file tab header, and tabbar label) to stay synchronized bidirectionally** — renaming from any surface must immediately reflect on all other surfaces. Treats any drift between these three surfaces as a bug. Confidence: 0.6

- **Sidebar items should be click-to-open in the workspace** — clicking a document/quick in the sidebar should open it as a tab in the workspace area (via `addTab`), not just select it in the sidebar. Left-click equals "open for editing". Confidence: 0.8

- **Smart display name fallback for sidebar documents** — sidebar document labels should derive from: (1) user-assigned name if set and non-empty, (2) first-line excerpt from document content (truncated to ~60 chars), (3) "Untitled" as final fallback. Never shows an empty/blank label. Confidence: 0.7

- **Quicks open in the home/quicks view, not the prompt builder** — clicking a quick document in the sidebar should load it into the quicks editing store and switch the main application view to "home". It must NOT open as a tab in the prompt builder workspace. Quicks are edited in-place in the dedicated quicks/home view, not as separate prompt builder tabs. Confidence: 0.9

- **"New" / create buttons must reset the store to an empty/clean state before navigating to the creation view** — pressing a "New Quick" (or similar create action) should clear all existing state (sections, output, name, savedDocId) via `store.reset()` before switching to the creation view, so the user gets a truly blank editor. Without this reset, stale state from a previously viewed document leaks into the new blank document. Confidence: 0.8

- **Prefers floating/flyover UI for transient tool panels (like Find & Replace)** — temporary tools should appear as floating overlays on top of content rather than pinned/fixed toolbars that shift the layout. The panel should be positioned relative to the trigger button and overlay the content beneath it. Confidence: 0.6

- **Wants floating action bars to stay out of the way when idle and reveal themselves contextually** — rather than an always-visible floating toolbar over the editor, the bar should hide at rest and "pop from the right corner" when the pointer enters that corner region, growing out of the corner. Prefers low-chrome, ambient controls that surface on approach over permanently docked toolbars. Confidence: 0.6

- **Prefers corner-anchored vertical "dial" action bars that fan out radially** — floating actions should originate from the bottom-right corner and stack **vertically** in a single column rather than sitting in a horizontal row, and — per the explicit clarification "radial lookout so it spans out like radial" — the actions must genuinely **span out along an arc**, not merely run straight up: their positions bow outward on a circle centred on the corner (the further from the pivot, the further off the edge), revealed with the whole group pivoting on the corner plus a staggered per-action offset so it reads as a fan/dial deploying rather than a list sliding. Radial geometry *and* radial choreography are both preferred over a straight column or linear slides for corner-anchored floating UI. Even the bowed column was judged to have "spaned out in wrong direction", and the desired motion for the primary action was expressed only through an interaction metaphor — "the main key comes with scroll kinda like camera radial scroll" — i.e. the primary should rotate/sweep into place like a lens/dial being scrolled rather than merely translate outward. This geometry stayed unresolved and was ultimately reverted back to the earlier committed horizontal floating bar, so the exact radial interaction the user wants is still unspecified. Confidence: 0.5

- **Click-outside-to-dismiss (click-off) for floating UI panels** — floating panels, popovers, and transient tool UIs should close automatically when the user clicks anywhere outside the panel. Dismissal should happen on `mousedown` for immediate feel, not on scroll or inside clicks. Confidence: 0.7

- **Enter key as confirmation action in tool panel input fields** — pressing Enter within an input field inside a tool panel (e.g., replace field in Find & Replace, rename input) should trigger the panel's primary action (replace all, commit rename, etc.), not just move focus. Escape should cancel/close. Confidence: 0.6

- **Find/replace tool must include case-sensitive and whole-word matching toggle buttons** — the user explicitly asked to "keep senstive and the other button there they were usefull." The find/replace UI should provide toggle buttons (lucide-react `CaseSensitive` and `WholeWord` icons) that modify the regex pattern: case-insensitive matching by default (add `i` flag when case toggle is off), and `\b` word-boundary wrapping when whole-word toggle is on. These toggles are considered essential utility controls, not optional extras. Confidence: 0.8

- **Settings stored in DB (fonts, heading sizes, theme) must be actively wired to the DOM** — persisted settings should not remain inert data; they must be applied as CSS custom properties on `documentElement` so they actually render visually. Uses `useEffect` in App.tsx to sync settings state to `--font-heading`, `--font-body`, `--font-mono`, `--heading-h1/2/3` CSS variables. Confidence: 0.8

- **Extends theming beyond dark/light via `data-theme` attribute and CSS custom properties** — prefers an extensible theme system where custom themes (e.g., cyberpunk) get their own `[data-theme="..."]` CSS variable block rather than being shoehorned into the `.dark`/`.light` class toggle. Confidence: 0.7



- **Prefers AI features to have holistic context awareness of the full project workspace** — AI chat/assistant should have access to the canvas, visual agent flows, designs, templates, snippets, and all project artifacts, not just isolated text content. The AI should "have full context of canvas and other reasoning" to generate meaningful outputs based on the user's visual work. Treats limited-context AI as needing enhancement to see the broader picture. Confidence: 0.7

- **AI assistant chat input should support `@`-mention syntax for ingesting snippets/library content as context** — the user should be able to type `@` in the AI chat input to resolve snippets, templates, or library references into the AI's context. This extends the existing `@`-mention snippet pattern to the AI assistant, enabling contextual ingestion of project artifacts into AI prompts. The resolved content should be passed as `libraryRefs` to the context builder. Explicitly requested: "user can ingest context with @ from the snippets." Confidence: 0.6

- **Prefers AI-generated outputs to stream directly into editing surfaces (builder, editor) rather than only displaying in a chat panel** — AI results should populate the document/editor inline as they're generated, enabling a fluid "generate and edit" workflow. The AI should be able to "stream the data into the builder" so the user can immediately see and iterate on the output within their content, not just in a separate assistant panel. Confidence: 0.6

- **Prefers AI chat assistant to be available ubiquitously across all editing views, not siloed to a single view** — wants "chat in quicks too" alongside the existing prompt builder integration. AI should be accessible from every editing surface (quicks, prompts, builder) as a contextual assistant. Confidence: 0.7

- **Canvas nodes must use a single shared accent color, not per-type colors** — all nodes (agent, action, condition, note) should use `var(--accent)` uniformly; rejects distinct accent colors per node type (e.g., `#5fd0ff` for action, `#ff9d5f` for condition). Nodes should be visually consistent, with type differentiation coming from the header label text, not colored borders/badges. Confidence: 0.7

- **Cyberpunk theme should be a terminal-green palette** — dark-green background (`#0d1b0e`), lime-green foreground/text (`#c8f135`), muted green secondary elements, dark green borders/surfaces. The "cyberpunk" aesthetic translates to monochrome green-on-dark with high-contrast accent. Confidence: 0.8

- **Outline/table-of-contents extraction must handle both JSONContent (Tiptap rich-text heading nodes) and compiled flat string output** — since freeform/quicks views pass compiled markdown text to the outline panel, it must regex-scan for `#` and `##` line prefixes in strings AND walk the Tiptap JSON tree for `heading` nodes in rich-text inputs. Both paths are needed depending on which editor mode is used. Confidence: 0.8

- **Outline entries must be clickable to navigate/scroll to the corresponding section in the editor** — clicking a heading/section entry in the outline panel should scroll the editor view to that section. The scroll strategy should be multi-layered: first try matching by text content against `[contenteditable]` editors, then against Tiptap heading elements (h1, h2), then against section title inputs, with smooth scroll behavior. The outline is considered incomplete without working scroll-to-navigation ("i love the outline just make it come to view when certain one is selected"). Confidence: 0.8

- **Expects transient visual confirmation on navigation actions — a brief (~1s) highlight on the destination content itself, not on the navigation UI** — when clicking something that scrolls content into view, plain scrolling is not enough: without a short-lived highlight the landing spot "feels lost." The highlight must land on the **scrolled-to target inside the document/editor**, not on the clicked outline/nav row — explicitly corrected with "it needs to be highlighted in the doc not in the outline" after the nav row and target were both highlighted; the nav row should return to plain hover styling. The flash is accent-tinted, theme-aware, and transient rather than a permanent selected state, and must be visible at the moment the smooth scroll settles (so it isn't already spent before the content arrives). Generalizes to any jump/navigation affordance. Confidence: 0.8

- **Uses React ErrorBoundary class components to protect critical navigation/layout from content render crashes** — critical chrome (tabbar, sidebar, app shell) should be wrapped outside the error boundary so that content-area render crashes don't cascade up and take down the entire UI. The error boundary is placed strategically around `<AppFlow />` (the content area) but below the tabbar/nav. Confidence: 0.7

- **Defensive array rendering with type guards before mapping over dynamically parsed data** — whenever content comes from parsing (paste, deserialization, async fetch), checks `Array.isArray(data)` and uses optional chaining (`item?.id || fallback`) before `.map()` to guard against undefined/null/object data shapes that would throw at runtime. Confidence: 0.7

- **Batches multiple Zustand store fields into a single `setState` call when ingesting data** — rather than calling separate setters (`store.setSections(result)` then `store.setHasContent(true)`), passes an object with all changed fields at once (`useQuicksStore.setState({ sections, output, hasContent })`) to prevent cascading re-renders from each individual mutation. Confidence: 0.7

- **Wraps async store persistence/serialization in try/catch to prevent non-critical save failures from crashing the UI** — `persist()` functions that serialize editor state (which may contain Tiptap JSON with circular references that `JSON.stringify` cannot handle) should be wrapped in try/catch so that save failures degrade gracefully without propagating up as unhandled promise rejections or triggering the React error boundary and unmounting the editor toolbar. Treats persistence as best-effort; a failed save should never take down the visible UI. Confidence: 0.7

- **Isolates `contentEditable` editors in separate components with local state + `useRef` to prevent React cursor-position reset** — when using `<div contentEditable>` or `<pre contentEditable>` in a React app where content originates from a Zustand store, the editable element must be wrapped in its own component that manages text locally via `useState` and directly manipulates the DOM via `useRef<HTMLElement>`. The component syncs to the parent store only through a callback (`onSave`) triggered on `onBlur` and `onInput` events, never by binding the store's value directly as the element's content via JSX. A `useEffect` syncs the DOM ref from the store only when the section ID changes (e.g., new document loaded). This prevents React reconciliation from resetting the cursor position on every keystroke, which was the fatal flaw of inlining `contentEditable` with store-driven content. Confidence: 0.8

- **Prefers `<pre contentEditable>` over `<textarea>` for non-scrollable editable content sections** — `<textarea>` required auto-grow hacks (`onInput` height-reset to `scrollHeight`) and `overflow-hidden` to appear seamless and non-scrollable, while `<pre contentEditable>` naturally displays all content without any clipping, scrollbars, or manual resize handles. The `<pre>` element preserves whitespace and monospace rendering inherently. Used with `white-space: pre-wrap` and `break-words` to handle line-wrapping. The user explicitly requested to move away from textarea ("make it work without textarea"). Confidence: 0.7

- **Uses synchronous `useStore.getState()` with fallback defaults for critical/initialization code** — when reading store state in top-level component initialization (e.g., App.tsx), prefers `useStore.getState().property` over the hook selector `useStore((s) => s.property)` because the hook throws if the store isn't mounted yet, crashing the entire app. Pairs this with safe fallback defaults (`raw?.property || defaultVal`) so the app renders even if the store is temporarily unavailable. Confidence: 0.7

- **Prefers structured, concise error logging in ErrorBoundary** — logs error message, error stack (truncated), and component stack (truncated) as separate `console.error` calls rather than dumping the full error object in one call. This keeps console output readable and ensures the message isn't buried in a massive serialized object. Confidence: 0.6

- **Diagnostic-first debugging — capture actual error evidence before applying any fix, using a branching classification to determine the root cause category** — when facing a hard-to-reproduce bug (e.g., TabBar disappearing on paste), insists on the following systematic protocol before any code changes: (1) Add `window.addEventListener('error', ...)` and `window.addEventListener('unhandledrejection', ...)` in App.tsx to catch errors that React ErrorBoundaries miss, (2) Make every ErrorBoundary log full `error.stack` and `info.componentStack` in `componentDidCatch`, (3) Reproduce the bug and inspect the console for the actual error message and stack trace, (4) Check the DevTools Elements panel to determine whether the broken UI element's DOM node is still present (CSS/layout issue) or completely removed (JS crash/React unmount). The branching diagnosis: if the DOM node is present but invisible/collapsed → CSS/layout issue (check computed styles on the element and its ancestor chain for `overflow`, `height`, `position`); if the DOM node is removed → unhandled throw escaping React boundaries, likely in a zustand subscriber, useEffect callback, or async code that ErrorBoundaries cannot catch. Explicitly rejects "blind mitigations" — "Don't apply another mitigation blind." Confidence: 0.9

- **Uses failed fix attempts as negative evidence to classify bugs by elimination** — when multiple attempts targeting a hypothesized root cause category (e.g., JS crashes via ErrorBoundaries, store init, imports) all fail to fix the bug, reasons that the bug must belong to a *different* category entirely (e.g., CSS/layout issue). Treats the pattern of 0% success across a category of fixes as strong diagnostic evidence that the original hypothesis was wrong, rather than continuing to apply "another mitigation blind." Confidence: 0.7

- **Prefers structural DOM rearchitecture over CSS positioning workarounds when `position: sticky` breaks** — when a sticky-positioned UI element disappears due to ancestor overflow contexts shifting after dynamic content changes, prefers restructuring the DOM hierarchy so the element becomes a flex/grid sibling *above* the scroll container (making sticky unnecessary via natural flow positioning), rather than debugging the overflow ancestry chain to preserve sticky. The stated motivation: "decouple from the shared ancestor's overflow context entirely" — sticky is only needed when the element scrolls with its container; if it's a flex sibling above the scroll area, plain flow positioning is inherently immune to the whole class of overflow-context bugs. Confidence: 0.6

- **Cannot read / will not inspect browser DevTools console output** — when asked to check the console for errors during debugging, responds "cant see shit there." This means the assistant must make all diagnostic information **visible directly in the application UI** (e.g., on-screen error overlays, fixed-position debug panels appended to the DOM body) rather than relying on the user reading console output or describing what they see in DevTools. Asking the user to paste console output is not a viable workflow. Confidence: 0.9

- **Enforces hard content size caps on paste/import to prevent browser freezes** — pasting very large amounts of content (e.g., 65k words) that would overwhelm Tiptap/React rendering should be truncated with hard limits on max sections and max characters per section rather than processed in full. Async deferral via `setTimeout(0)` alone is insufficient — explicit caps are required to keep the editor responsive under large-input scenarios. Confidence: 0.7

- **Prefers preserving markdown-to-rich-text parsing on paste even for large content** — explicitly rejected the approach of bypassing `markdownToDoc()` / `mdToHtml()` formatting pipeline in favor of raw text. Wants proper rich-text formatting (headings, bold, lists) preserved from pasted content. Performance issues from large pastes should be solved with better content size caps, not by removing the parsing pipeline. Confidence: 0.8

- **Prefers borderless content sections in the builder — sections should not be wrapped in visible border/container boxes** — visual separation between sections should come from the heading label and spacing alone, not from enclosing border divs. Each section's editor content should span the full available width of its parent container without inner border constraints. Confidence: 0.7

- **Designs for LLM-agent-scale content as a core product constraint** — expects the app to handle large content volumes (80K+ tokens) natively because "people will put their agents here." Content ingestion and rendering should not be a bottleneck; this is a first-class architectural requirement, not an optimization to defer. Confidence: 0.8

- **Pragmatic acceptance of lazy/progressive rendering for large content** — willing to accept lazy mounting (rendering content sections in staggered batches rather than all at once), async deferral, and other progressive loading approaches to keep the UI responsive when handling large pastes/imports. Does not demand instant full-render of all content. Confidence: 0.7

- **Content parsing/import must never silently truncate user data** — explicit caps on parse/import are acceptable for performance, but if they cause content to go missing ("only parsing half the content half content is gone"), the user will notice and complain immediately. Caps must be generous enough to handle realistic LLM-agent content volumes, and any truncation must be clearly communicated rather than silent data loss. The 80K total cap on `parseMarkdownSections` was rejected because it silently discarded user content. Confidence: 0.7

- **Prefers lucide-react icon components over emoji/unicode characters throughout the UI** — emoji characters (🔍, ↺, ✓, ↗, ≡, etc.) should be replaced with equivalent lucide-react icon components (`Search`, `Undo2`, `Check`, `ArrowUpRight`, `ListTree`, etc.). The semantic meaning of the original icon should be preserved — e.g., the "generate" action uses `ArrowUpRight` (not `Sparkles`), and arrow icons remain arrows. This applies consistently to all button icons, toggle indicators, and action symbols. Confidence: 0.7

- **Prefers `<textarea>` over `<div contentEditable>` for store-driven editor content in Zustand-based apps** — when editor content is stored in a Zustand store and updated on every keystroke (e.g., `onChange`/`onInput` writing to store), `<div contentEditable>` breaks because React reconciliation resets the cursor position on each re-render, making typing impossible. A `<textarea>` is stable and reliable for this pattern. Uses `<textarea>` with `resize-none`, transparent background, and `font-mono` to minimize visual congestion. The `contentEditable` approach only works for editors that avoid React re-renders on input (e.g., Tiptap which manages its own DOM internally). Confidence: 0.8

- **Textareas in quicks/home view must be non-scrollable and show ALL content without clipping** — textareas should have no internal scrollbar (`overflow-hidden` or auto-grow). Fixed viewport-relative heights (`calc(100vh - 280px)`) were rejected because they clip content when the content is larger than the viewport. The correct approach is **auto-grow via `onInput`**: reset `el.style.height = "auto"` then set `el.style.height = el.scrollHeight + "px"` so the textarea expands to fit its content and the outer container scrolls the whole page. Also removes the manual resize handle (`resize-none`). Confidence: 0.8

- **Content ingestion must preserve exact byte-for-byte fidelity — no silent content modification** — content parsed/pasted into the app must be preserved exactly as-is, including leading/trailing whitespace and newlines. Applying `.trim()` to section bodies silently alters character counts, which the user catches by comparing against Notepad ("on note pad its 44k its 35 here"). The character count shown must match the actual input character count exactly. This extends beyond "no truncation" — even transformations like `.trim()` that remove whitespace are unacceptable because they break content integrity and erode user trust. Confidence: 0.8

- **Prefers auto-focus on newly created content editors over placeholder text** — when an empty editor is created with no placeholder text visible, prefers the editor to automatically receive focus so the cursor lands there immediately, making a visual placeholder unnecessary. The auto-focus should target only the first (or only) section editor when a new document is created. Confidence: 0.7

- **Rich text rendering (via Tiptap) is a non-negotiable requirement for ALL editor surfaces including home/quicks — explicitly rejects raw contentEditable substitutes that lose formatting and theme integration** — when the assistant replaced Tiptap `SmartEditor` with `<pre contentEditable>` in the home/quicks view, the user immediately objected that "this content is only parsing heading its not replacing into the tiptap document we have been using with controlled themes and everything." The user considers Tiptap's rich text rendering (headings rendered as styled elements, theme CSS variables applying, full formatting support) a core requirement, not something that can be substituted with a simpler DOM-editing approach anywhere in the app. Even if a raw contentEditable or textarea approach seems functionally adequate for plain text, it must be rejected in favor of Tiptap because it cannot render rich text (headings, bold, lists) or apply theme styling. The correct solution is using Tiptap per section with size caps, not abandoning Tiptap. Confidence: 0.8

- **Uses a key-counter state to force React re-mount of editors that read `initialContent` on mount when the underlying store content changes** — when a store mutation (e.g., "Replace All" in find/replace) updates section content in a Zustand store, but each editor component only reads `initialContent` from the store once on mount (typical Tiptap pattern), the solution is to: (1) batch-update the store with `setState({ sections: updated })`, (2) increment a counter state (e.g., `rectifyKey`), and (3) append the counter to the editor's React `key` prop (`key={section.id + '-' + rectifyKey}`). This forces React to unmount and remount all editor instances, causing them to re-read the fresh content from the store. Relies on key-driven re-mounting rather than attempting to make editors reactively update their internal content. Confidence: 0.7

- **Maintains a `decision-logs.md` as a living decisions/status document and expects the agent to consult and update it** — the user keeps the project's decision log open and refers the agent to it to see outstanding work ("check whats left here"), implying it tracks decisions plus a "Known Issues (Next Session)" backlog. Expects the agent to verify what is still open vs. already fixed and to append a session entry documenting changes/decisions. Confidence: 0.6

- **Native `<select>` elements are not acceptable — all dropdowns must use the app's custom dropdown component** — explicitly requires "the drop downs need to match the other drop downs the custom ones." Any newly added dropdown (selects for scope, group, template, pickers, etc.) must be built on / match the existing bespoke custom dropdown pattern (portal-rendered panel, `bg-surface` / `border-border` / `shadow-lg` / `rounded-xl`, motion fade + rotating chevron, dismiss on mousedown-outside and Escape, viewport-clamped) rather than a plain HTML `<select>`. Reusability is expected: when no shared component exists, extract the established pattern (e.g., `TemplateSelector`) into a reusable UI component instead of duplicating inline markup. Consistency of interactive controls across the app outweighs convenience. Confidence: 0.8

- **Expects outstanding "known issues" to be closed out rather than left deferred** — when told "fix all the running issues", the user wants every open item in the decision log/known-issues list addressed (not just the currently requested feature), and the log updated to reflect the resolved status. Dropping previously deferred open issues in scope is acceptable and expected; does not want them tracked indefinitely without being fixed. Confidence: 0.6

- **Graph views must be a real relationship node-graph (Obsidian-style), not a hierarchical tree** — explicitly: "graph needs to be a actual graph of nodes like obsidian". Wants nodes = entities and edges = relationships between them (e.g., skills linked by shared group / shared template), laid out spatially with pan/zoom, rather than the previous parent-child tree/hierarchy layout. Prefers this force-directed, explorable graph representation for visualizing connections. Confidence: 0.6

- **Rejects UIs where items only appear once placed in a group/folder — ungrouped/uncategorized items must remain visible by default** — complained that "when skills are ungrouped they are not visible" and that it "seems like base folder only accepts other folder". Any grouping/tree structure must surface uncategorized items (e.g., an explicit "Ungrouped" section) rather than hiding them behind group membership. Also expects deleting a group to not silently discard its contents. Confidence: 0.6

- **Prefers normalizing content to a single canonical format instead of maintaining parallel/special-case representations** — when multiple representations of the same content coexist (e.g., a `JSONContent | string` union, custom serializers, and a section model), wants them collapsed to one canonical representation per boundary ("we can just normalize it for better experience"). Favors markdown as the source-of-truth storage format with structured views (sections, plain/JSON/XML) *derived* from it rather than stored as separate hand-rolled models. Confidence: 0.6

- **Prioritizes product simplicity — willing to cut features/abstractions that add tedium without clear payoff** — self-describes the goal as "simple tho" and is ready to drop the section model for quicks because its only justification (reordering) is "tedious" and "not so useful." Prefers eliminating the abstraction over keeping it for marginal functionality, even when that means overruling a previously recorded architectural preference. Confidence: 0.7

- **Reports bugs in terse, non-technical, visual-symptom terms rather than with reproduction steps or error details** — e.g., "its all blank and white and flashy" for what turns out to be a possible render crash/loop. The report carries no stack trace, console output, or the exact action that triggered the failure, so the agent must infer the likely failure category (render crash vs. layout/CSS vs. logic) from a vague holistic description and proactively investigate the relevant wiring (styles, theme, module graph) rather than waiting for precise repro info. The same applies to visual/motion feedback: an unsatisfying animation is reported only as a direction complaint ("it spaned out in wrong direction") plus an interaction metaphor, with no coordinates or measurements — the agent is expected to either ask focused clarifying questions (ideally with visual/ASCII mockups of the candidate layouts) or infer the intent rather than guess silently. Confidence: 0.65

- **Wants the agent to state up front what it is about to do before doing it** — explicitly asks "let me know what are you trying to do" so the user can follow the plan and know what to expect. Prefers a short statement of intent/proposed change before a long investigation or edit sequence, rather than the agent working silently through many diagnostic steps. Confidence: 0.6

- **Runs the Tauri dev server (`npm run tauri dev`) himself and expects the agent not to occupy the app's dev port (1420)** — the agent must clean up any background Vite/dev server it started, because a leftover server on 1420 blocks the user's `tauri dev` ("Port 1420 is already in use"). Leave the dev server for the user to run, and free the port before handing off. Confidence: 0.6

- **Tolerates leaving a minor cosmetic bug unresolved and moves on to the next request** — after a non-critical polish fix (a transient navigation highlight) still didn't work, responds with "nope not doing it but its okay ig if not working" and immediately pivots to a new feature request in the same message. Doesn't want cycles sunk into a low-impact nicety once it has resisted a couple of attempts; the agent should accept the abandonment and switch tasks rather than escalating the diagnosis. Confidence: 0.6

- **Prefers delivering the fix over prolonged diagnosis, and volunteers to be the manual tester** — when debugging drags on, pushes the agent to stop investigating and ship the fix ("just fix the platform and paste"), offering to verify it themselves ("I will test it"). The agent should communicate the fix/deliverable and hand off verification to the user instead of continuing extended headless root-cause exploration. Confidence: 0.6